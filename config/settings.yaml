simulation:
  timestep_hours: 1.0
  max_iterations: 8760  # One full year of hours
  fallback_mode: true

grid:
  max_import_kw: 100.0
  max_export_kw: 50.0
  buy_price_kwh: 0.15
  sell_price_kwh: 0.08

features:
  enable_cfd_wind_sim: false
  enable_mppt_tracking: true
